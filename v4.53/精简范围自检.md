# v4.53 精简范围自检

- 版本目录包含 1 个中文名 Shadowrocket 模块文件、1 个中文名京东启动脚本和 3 份中文 Markdown 记录。
- URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六段配置结构保持存在。
- `[Rule]` 新增 1 条全局 `URL-REGEX` 请求级规则，不依赖 MITM。
- 全局正则使用开屏特征边界，覆盖 `splashScreenInfo`，不匹配 `startupConfig`、普通首页和普通图片路径。
- 全局规则不包含 `DOMAIN-SUFFIX`、`DOMAIN-KEYWORD` 或 `hostname = *`。
- `[Map Local]` 原有全局响应兜底仍保留，已纳入 MITM 的软件可以继续响应改写。
- 京东本地脚本引用指向 v4.53，京东直连例外和纵横小说规则保持存在。
- 未新增 v4.7 全量域名，未修改 v4.52 及之前版本目录。
- 已完成文本静态检查；所有软件冷启动和网络功能仍需 iPhone/Shadowrocket 实机验证。
