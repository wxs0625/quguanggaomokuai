# v4.51 精简范围自检

- 版本目录包含 1 个中文名 Shadowrocket 模块文件、1 个中文名京东启动脚本和 3 份中文 Markdown 记录。
- URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六段配置结构保持存在。
- 京东直连例外位于 `[Rule]` 段前部，覆盖 `dns.jd.com`、`policy.jd.com`、`im-x.jd.com`、`101.124.19.122/32` 和 `2402:db40:5100:1011::5/128`。
- 集成文件不再包含 `DOMAIN-SUFFIX,policy.jd.com,REJECT`；其余京东广告域名规则保持存在。
- 京东远程脚本不再匹配 `start`；本地 `京东启动去广告.js` 单独匹配 `functionId=start`，支持 `http/https` 和查询参数。
- 京东本地脚本只清空启动响应的 `images` 并关闭 `showTimesDaily`，空响应和解析失败不改写。
- 纵横启动规则支持任意 `zongheng.com` 子域、`http/https` 和可选查询参数，并返回 `reject-200`。
- 纵横礼包规则支持可选查询参数和 `api`、`api1`、`api2` 主机，并返回 `reject-dict`。
- 未新增 v4.7 全量域名，未修改 v4.50 及之前版本目录。
- 已完成文本静态检查；iPhone/Shadowrocket 冷启动和广告展示仍需实机验证。
