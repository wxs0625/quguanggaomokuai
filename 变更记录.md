# v4.17 - 闪动校园 HTTPS 开屏广告修复版

- 日期：2026-08-13
- 文件夹：v4.17/
- 模块文件：v4.17/全能去广告修复版-Shadowrocket-v4.17.sgmodule
- 用户反馈：v4.16 可以进入但开屏广告依旧存在。
- 核心发现：v4.16 虽恢复 getAd 空数据，但 ad.shunchangzhixing.com 仍被 MITM 排除，HTTPS 开屏广告接口无法被 Map Local 命中。
- 核心修复：恢复 ad.shunchangzhixing.com 正向 MITM，并新增 ad.shunchangzhixing.com/base/* 空对象映射；保留启动稳定白名单。
- 稳定性策略：不封禁整个 shunchangzhixing.com，不动 hzjizhun/gromore/getui 等启动稳定接口。

# v4.16 - 闪动校园稳定去开屏广告版

- 日期：2026-08-13
- 文件夹：v4.16/
- 模块文件：v4.16/全能去广告修复版-Shadowrocket-v4.16.sgmodule
- 用户反馈：v4.15 可以进入闪动校园，但有开屏广告。
- 核心修复：禁用过宽的 USER-AGENT,ShanDong*,DIRECT，保留明确启动 SDK 白名单，并仅对 ad.shunchangzhixing.com/getAd 返回空数组。
- 稳定性策略：不拦截 base/infoConfig/checkCd/gromore 等上一轮用于启动稳定的接口，避免回到闪退。

# v4.15 - 闪动校园启动 SDK 稳定版

- 日期：2026-08-13
- 文件夹：v4.15/
- 模块文件：v4.15/全能去广告修复版-Shadowrocket-v4.15.sgmodule
- 本次依据：用户提供的新 PacketTunnel 日志中，21:02:57 后 v4.14 白名单已经命中 DIRECT，但后续启动 SDK 仍继续出现 REJECT。
- 核心发现：v4.14 修掉了 shunchangzhixing / hzjizhun / gromore / getui 这一层；剩余闪退更可能来自广告 SDK 初始化链路继续被拦截。
- 核心修复：新增 USER-AGENT,ShanDong*,DIRECT，并放行 v4.14 生效后仍被拒绝的 umeng、amap、zhangyuyidong、sigmob、gdt、e.qq、ubixioe、adfunlink、1rtb、pangolin 等启动期域名。
- 说明：本版本稳定优先，可能牺牲闪动校园内部分广告屏蔽效果；先验证不闪退，再逐条恢复广告拦截。

# v4.14 - 闪动校园日志修复版

- 日期：2026-08-13
- 文件夹：v4.14/
- 模块文件：v4.14/全能去广告修复版-Shadowrocket-v4.14.sgmodule
- 本次依据：用户提供的 PacketTunnel-20260813200555.log，闪动校园 UA ShanDong/8.6.6 在启动阶段多次命中 REJECT。
- 核心发现：api.huachenjie.com 在日志中已是 DIRECT，而 ad.shunchangzhixing.com/base/*、admin.hzjizhun.cn/*、gromore.pangolin-sdk-toutiao.com 和个推域名存在启动阶段拦截。
- 核心修复：新增启动直连白名单，禁用 ad.shunchangzhixing.com/getAd 本地改写，并从 MITM hostname 中排除相关启动域名。
- 说明：本版本为稳定优先版本，用于验证闪退/卡启动页是否由启动 SDK 被拦截引起；确认稳定后再继续小步恢复广告屏蔽。

# 广告模块变更记录

本仓库以后按“小版本”记录每次改动。主模块使用 `v4.x`；当前维护只推荐主模块，单独 App 修复模块仅在历史版本目录中归档保留。

## 当前文件

| 文件 | 版本 | 用途 | 状态 |
| --- | --- | --- | --- |
| `v4.13/全能去广告修复版-Shadowrocket-v4.13.sgmodule` | v4.13 | 主合并模块，适用于 Shadowrocket，基于 v4.7 可启动基线的闪动校园最小回滚稳定版 | 当前推荐 |

## 目录规范

- 根目录下每个小版本独立一个文件夹，例如 `v4.0`、`v4.1`、`v4.2`。
- 每个版本文件夹保留该版本模块文件，不再删除历史版本。
- 每个版本文件夹必须包含 `本版本变更记录.md`，用于后续检查。
- GitHub 上传必须先确认仓库地址并取得明确授权。

## v4.13 - 2026-08-13

### 改动摘要

- 新增 `v4.13` 目录，保留本版本主模块文件和本版本变更记录。
- 回到用户实测可启动的 `v4.7` 作为基线，不沿用 `v4.8` 到 `v4.12` 的扩大改动。
- 只取消 `api.huachenjie.com` 主业务接口劫持：注释 3 条 `[Map Local]`，并从 `[MITM]` 移除 `api.huachenjie.com`。
- 保留 `v4.7` 中已实测可启动的闪动校园启动广告规则，包括 `ad.shunchangzhixing.com`、`open.e.kuaishou.cn`、`zlsdk.1rtb.net`、`sdk.1rtb.net`。
- 本版本目标是解决 `v4.7` 进入后无网络，同时不引入后续版本导致进不去软件的变量。

### 静态校验结果

- `api.huachenjie.com` 活跃 `[Map Local]` 命中数：`0`。
- `api.huachenjie.com` 在 `[MITM]` 主机列表命中数：`0`。
- 启动广告相关 5 个域名命中与 `v4.7` 保持一致。
- 文件为无 BOM UTF-8，首字节为 `#!name`。

### 当前链接

- GitHub 文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.13/全能去广告修复版-Shadowrocket-v4.13.sgmodule`
- Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.13/全能去广告修复版-Shadowrocket-v4.13.sgmodule`

## v4.12 - 2026-08-13

### 改动摘要

- 新增 `v4.12` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.11` 复制生成 `v4.12/全能去广告修复版-Shadowrocket-v4.12.sgmodule`，旧版本不删除。
- 明确原因：`v4.7` 是主业务接口 `api.huachenjie.com` 被改写导致无网络；后续版本是启动广告 SDK 仍被拒绝、改写、脚本或 MITM 处理导致卡启动页/闪退风险。
- 修正此前只加 `DIRECT` 的不足：`DIRECT` 不能阻止 URL Rewrite、Map Local、Script、MITM 继续命中。
- 对闪动校园核心域名和高风险启动 SDK 做隔离稳定处理：只保留顶部 `DIRECT`，移除拒绝、改写、脚本和 MITM 命中。
- 合计清理风险命中 `235` 条/项，其中 `[Rule]` 208 条、`[URL Rewrite]` 15 条、`[Body Rewrite]` 5 条、`[Map Local]` 6 条、`[MITM]` 1 组。

### 静态校验结果

- 风险域名只出现在顶部 `DIRECT` 白名单。
- 风险域名在 `reject`、`REJECT`、`Map Local`、`Script`、`hostname =` 中残留命中均为 `0`。
- 模块文件为无 BOM UTF-8，首字节为 `#!name`。
- 模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。

### 当前链接

- GitHub 文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.12/全能去广告修复版-Shadowrocket-v4.12.sgmodule`
- Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.12/全能去广告修复版-Shadowrocket-v4.12.sgmodule`

## v4.11 - 2026-08-13

### 改动摘要

- 新增 `v4.11` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.10` 复制生成 `v4.11/全能去广告修复版-Shadowrocket-v4.11.sgmodule`，旧版本不删除。
- 针对用户反馈：`v4.10` 仍偶发闪退但成功进入次数增加，判断还有随机启动广告 SDK 被通用规则/MITM 命中。
- 在 `[Rule]` 顶部新增 `seeyouyima.com`、穿山甲/字节 SDK、腾讯 GDT 相关域名 `DIRECT` 白名单。
- 从 `[URL Rewrite]`、`[Map Local]`、`[Script]` 暂停 30 条高风险 SDK 改写/脚本命中。
- 从 `[MITM]` 主机列表移除 30 个高风险启动 SDK 主机，降低启动页卡住或闪退概率。
- 百度网盘补齐 7 条实际 `[URL Rewrite]` 开屏广告拦截规则，修正此前只有 MITM 主机、缺少实际拦截规则的问题。

### 闪动校园新增保守直连

- `seeyouyima.com`
- `pangle.io`
- `pangolin-sdk-toutiao.com`
- `pangolin-sdk-toutiao-b.com`
- `pangolin-sdk-toutiao1.com`
- `pglstatp-toutiao.com`
- `pstatp.com`
- `snssdk.com`
- `sgsnssdk.com`
- `zijieapi.com`
- `byteimg.com`
- `ibyteimg.com`
- `amemv.com`
- `gdt.qq.com`
- `gdtimg.com`
- `qzs.qq.com`

### 百度网盘新增规则

- `pan.baidu.com/act/api/activityentry`
- `pan.baidu.com/rest/2.0/pcs/adx`
- `pan.baidu.com/api/useractivity/activity`
- `update.pan.baidu.com/statistics`
- `issuecdn.baidupcs.com/issue/netdisk/guanggao/`
- `ndstatic.cdn.bcebos.com/activity/welfare/js/`
- `afd.baidu.com/afd/entry`

### 当前链接

- GitHub 文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.11/全能去广告修复版-Shadowrocket-v4.11.sgmodule`
- Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.11/全能去广告修复版-Shadowrocket-v4.11.sgmodule`

### 校验结果

- 已保留 `v4.0` 至 `v4.10` 历史版本目录。
- `v4.11` 目录已包含模块文件和 `本版本变更记录.md`。
- 模块文件为无 BOM UTF-8，首字节为 `#!name`。
- 模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。

## v4.10 - 2026-08-13

### 改动摘要

- 新增 `v4.10` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.8` 复制生成 `v4.10/全能去广告修复版-Shadowrocket-v4.10.sgmodule`，并重写为无 BOM UTF-8。
- 针对用户反馈：`v4.7` 闪动校园进入后无网络，`v4.8` 与 `v4.9` 无法进入软件。
- 闪动校园改为完全直连稳定版：在 `[Rule]` 顶部加入核心域名和启动广告 SDK 域名 `DIRECT` 白名单。
- 清理闪动校园相关规则、URL Rewrite、Map Local、Script 和 MITM 主机命中项，避免通用去广告规则继续误伤启动或联网。
- 继续保留 v4.8 的百度网盘开屏广告补强规则。

### 闪动校园直连白名单

- `huachenjie.com`
- `shunchangzhixing.com`
- `kuaishou.cn`
- `kuaishou.com`
- `ksapisrv.com`
- `gifshow.com`
- `adukwai.com`
- `1rtb.net`

### 当前链接

- GitHub 文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.10/全能去广告修复版-Shadowrocket-v4.10.sgmodule`
- Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.10/全能去广告修复版-Shadowrocket-v4.10.sgmodule`

### 校验结果

- 已保留 `v4.0` 至 `v4.9` 历史版本目录。
- `v4.10` 目录已包含模块文件和 `本版本变更记录.md`。
- 模块文件为无 BOM UTF-8，首字节为 `#!name`。
- 模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。

## v4.9 - 2026-08-13

### 改动摘要

- 新增 `v4.9` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.8` 复制生成 `v4.9/全能去广告修复版-Shadowrocket-v4.9.sgmodule`。
- 闪动校园改为启动稳定优先：暂停闪动校园相关 Map Local 响应体改写。
- 删除闪动校园启动阶段可能命中的 `ad.shunchangzhixing.com`、`open.e.kuaishou.cn`、`zlsdk.1rtb.net`、`sdk.1rtb.net` 本地响应改写。
- 从 MITM 主机列表中移除 `ad.shunchangzhixing.com`、`open.e.kuaishou.cn`、`open.e.kuaishou.com`、`zlsdk.1rtb.net`、`sdk.1rtb.net`，避免启动阶段 HTTPS 解密误伤。
- 从通用广告脚本匹配中移除 `open.e.kuaishou.com/rest/e/v3/open/univ`，避免与其它模块的 MITM 主机叠加后误触发。
- 继续保留 v4.8 的百度网盘开屏广告补强规则。

### 当前链接

- GitHub 文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.9/全能去广告修复版-Shadowrocket-v4.9.sgmodule`
- Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.9/全能去广告修复版-Shadowrocket-v4.9.sgmodule`

### 校验结果

- 已保留 `v4.0` 至 `v4.8` 历史版本目录。
- `v4.9` 目录已包含模块文件和 `本版本变更记录.md`。
- 模块文件为无 BOM UTF-8，首字节为 `#!name`。
- 模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。

## v4.8 - 2026-08-13

### 改动摘要

- 新增 `v4.8` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.7` 复制生成 `v4.8/全能去广告修复版-Shadowrocket-v4.8.sgmodule`。
- 闪动校园改为保守兼容：取消对 `api.huachenjie.com` 的 HTTPS MITM 和 Map Local 改写，避免打开显示“无网络连接”。
- 保留闪动校园外部广告 SDK 处理：`ad.shunchangzhixing.com`、`open.e.kuaishou.cn`、`zlsdk.1rtb.net`。
- 百度网盘补充 `pan.baidu.com` 活动入口、会员弹层、`pcs/adx`、`update.pan.baidu.com/statistics`、`ndstatic.cdn.bcebos.com/activity/welfare/js/` 等开屏广告相关规则。

### 当前链接

- GitHub 文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.8/全能去广告修复版-Shadowrocket-v4.8.sgmodule`
- Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.8/全能去广告修复版-Shadowrocket-v4.8.sgmodule`

### 校验结果

- 已保留 `v4.0` 至 `v4.7` 历史版本目录。
- `v4.8` 目录已包含模块文件和 `本版本变更记录.md`。
- 模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。

## v4.7 - 2026-08-13

### 改动摘要

- 新增 `v4.7` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.6` 复制生成 `v4.7/全能去广告修复版-Shadowrocket-v4.7.sgmodule`。
- 本版本不修改广告拦截逻辑，仅记录 GitHub 上传完成状态。
- 根据你明确回复“授权上传”后的实际操作，完成 GitHub 推送。
- SSH 推送因本机 GitHub 公钥未授权失败，已改用 HTTPS 远端成功上传。

### GitHub 上传结果

- 目标仓库页面：`https://github.com/wxs0625/quguanggaomokuai`
- 当前远端：`https://github.com/wxs0625/quguanggaomokuai.git`
- 当前分支：`main`
- 首次上传 commit：`6631440 更新 GitHub 仓库地址 v4.6`
- 上传完成记录 commit：以当前最新提交为准，可通过 `git log -1 --oneline` 查看。
- 当前文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.7/全能去广告修复版-Shadowrocket-v4.7.sgmodule`
- 当前 Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.7/全能去广告修复版-Shadowrocket-v4.7.sgmodule`

### 闪动校园模块说明

- 当前推荐文件只有主模块 `v4.7/全能去广告修复版-Shadowrocket-v4.7.sgmodule`。
- 闪动校园兼容修复已内置在主模块中。
- `v4.2` 目录里的闪动校园单独模块只是历史归档，不作为当前推荐使用。

### 校验结果

- 已保留 `v4.0` 至 `v4.6` 历史版本目录。
- `v4.7` 目录已包含模块文件和 `本版本变更记录.md`。
- GitHub 仓库已成功建立 `main` 分支并上传本地版本历史。

## v4.6 - 2026-08-13

### 改动摘要

- 新增 `v4.6` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.5` 复制生成 `v4.6/全能去广告修复版-Shadowrocket-v4.6.sgmodule`。
- 本版本不修改广告拦截逻辑，仅更新版本号、说明和 GitHub 上传目标仓库。
- 将目标仓库从 `wxs0625/-` 更新为 `wxs0625/quguanggaomokuai`。
- 明确上传命令等待你授权后再执行，当前不配置远端、不推送。

### GitHub 上传准备

- 目标仓库页面：`https://github.com/wxs0625/quguanggaomokuai`
- 授权后计划分支：`main`
- 授权后计划命令：`git remote add origin git@github.com:wxs0625/quguanggaomokuai.git`、`git branch -M main`、`git push -u origin main`
- 预计文件页：`https://github.com/wxs0625/quguanggaomokuai/blob/main/v4.6/全能去广告修复版-Shadowrocket-v4.6.sgmodule`
- 预计 Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/quguanggaomokuai/main/v4.6/全能去广告修复版-Shadowrocket-v4.6.sgmodule`

### 闪动校园模块说明

- 当前推荐文件只有主模块 `v4.6/全能去广告修复版-Shadowrocket-v4.6.sgmodule`。
- 闪动校园兼容修复已内置在主模块中。
- `v4.2` 目录里的闪动校园单独模块只是历史归档，不作为当前推荐使用。

### 校验结果

- 已保留 `v4.0` 至 `v4.5` 历史版本目录。
- `v4.6` 目录已包含模块文件和 `本版本变更记录.md`。
- 本版本未执行 GitHub 上传，等待明确授权。

## v4.5 - 2026-08-13

### 改动摘要

- 新增 `v4.5` 目录，保留本版本主模块文件和本版本变更记录。
- 从 `v4.4` 复制生成 `v4.5/全能去广告修复版-Shadowrocket-v4.5.sgmodule`。
- 本版本不修改广告拦截逻辑，仅更新版本号、说明和 GitHub 上传准备文档。
- 在 `GitHub上传说明.md` 中记录目标仓库 `git@github.com:wxs0625/-.git`。
- 明确上传命令等待你授权后再执行，当前不配置远端、不推送。

### GitHub 上传准备

- 目标仓库页面：`https://github.com/wxs0625/-`
- 授权后计划分支：`main`
- 授权后计划命令：`git remote add origin git@github.com:wxs0625/-.git`、`git branch -M main`、`git push -u origin main`
- 预计文件页：`https://github.com/wxs0625/-/blob/main/v4.5/全能去广告修复版-Shadowrocket-v4.5.sgmodule`
- 预计 Raw 订阅链接：`https://raw.githubusercontent.com/wxs0625/-/main/v4.5/全能去广告修复版-Shadowrocket-v4.5.sgmodule`

### 闪动校园模块说明

- 当前推荐文件只有主模块 `v4.5/全能去广告修复版-Shadowrocket-v4.5.sgmodule`。
- 闪动校园兼容修复已内置在主模块中。
- `v4.2` 目录里的闪动校园单独模块只是历史归档，不作为当前推荐使用。

### 校验结果

- 已保留 `v4.0` 至 `v4.4` 历史版本目录。
- `v4.5` 目录已包含模块文件和 `本版本变更记录.md`。
- 本版本未执行 GitHub 上传，等待明确授权。

## v4.4 - 2026-08-13

### 改动摘要

- 将历史版本整理为根目录版本文件夹。
- 新增 `v4.0`、`v4.1`、`v4.2`、`v4.3`、`v4.4` 目录。
- 每个版本目录都包含该版本模块文件和 `本版本变更记录.md`。
- 恢复 v4.2 中曾经存在的闪动校园单独测试模块，仅作为历史归档保留。
- 当前推荐版本升级为 `v4.4/全能去广告修复版-Shadowrocket-v4.4.sgmodule`。

### GitHub 上传约定

- 本次只完成本地整理，不上传 GitHub。
- 需要你提供 GitHub 仓库地址并明确授权后，才执行 `git remote add` 或 `git push`。
- 上传后会在记录里补充 commit、远端地址和 GitHub 文件访问链接。

### 校验结果

- 历史版本均已保留在对应版本目录。
- 当前推荐文件仅保留一个主模块。
- 主模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。

## v4.3 - 2026-08-13

### 改动摘要

- 删除单独发布的 `闪动校园兼容修复版-Shadowrocket-v1.1.sgmodule`。
- 将主模块从 `v4.2` 升级为 `v4.3`，文件名改为 `全能去广告修复版-Shadowrocket-v4.3.sgmodule`。
- 闪动校园兼容修复继续保留在主模块的 `[Map Local]` 中，不需要再单独订阅一个模块。

### 原因

- 单独模块只是用于隔离测试闪动校园问题，正式发布时容易让人误以为需要同时导入两个模块。
- 现在统一为单文件发布，后续小版本只维护主模块和本变更记录。

### 校验结果

- 当前推荐文件仅保留一个主模块。
- 闪动校园相关接口仍返回带状态字段的安全 JSON。
- 主模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。

## v4.2 - 2026-08-13

### 改动摘要

- 将主模块发布文件改为中文命名：`全能去广告修复版-Shadowrocket-v4.2.sgmodule`。
- 将闪动校园单独模块改为中文命名：`闪动校园兼容修复版-Shadowrocket-v1.1.sgmodule`。
- 建立小版本记录规范：后续每次可见改动递增一个小版本，并在本文件追加说明。
- 保留 v4.1 中对闪动校园的兼容修复，避免空 JSON 响应导致 App 闪退。
- 保留对广告 SDK 接口的脚本或 Map Local 处理，避免规则层硬拦截抢先触发。

### 闪动校园兼容修复

涉及域名：

- `api.huachenjie.com`
- `ad.shunchangzhixing.com`
- `open.e.kuaishou.cn`
- `zlsdk.1rtb.net`
- `sdk.1rtb.net`

处理方式：

- 原先多个接口直接返回 `data="{}"`。
- 现在改为返回带状态字段的空列表或空广告结构，例如 `code`、`msg`、`data`、`ads`、`ad_infos`。
- 这样既能去掉广告内容，也能减少 App 因缺少预期字段而闪退的概率。

### 冲突修复

已从主模块中移除这些会抢先命中的硬拦截：

- `DOMAIN,open.e.kuaishou.com,REJECT`
- `DOMAIN-SUFFIX,e.kuaishou.com,REJECT`
- `DOMAIN-SUFFIX,zlsdk.1rtb.net,REJECT`
- `sdk.1rtb.net` 的 `reject-dict` URL Rewrite
- `open.e.kuaishou.com/rest/e/v3/open/univ` 的直接 reject

原因：这些接口后续已有脚本或 Map Local 处理，硬拦截优先命中时会导致兼容修复不生效，严重时可能让 App 启动闪退。

### 校验结果

- 主模块结构段落完整：`[Rule]`、`[URL Rewrite]`、`[Body Rewrite]`、`[Map Local]`、`[Script]`、`[Header Rewrite]`、`[MITM]`。
- 闪动校园新增响应体均可解析为 JSON。
- 与闪动校园相关的硬拦截冲突数为 `0`。
- 单独闪动校园模块中已无 `data="{}"`。

## v4.1 - 2026-08-13

### 改动摘要

- 基于 fmz200 的 `README-RULE.md` 汇总规则，生成 Shadowrocket 兼容模块。
- 引入闪动校园相关去广告规则。
- 初步修复闪动校园打开后闪退问题。

### 已知问题

- 部分广告 SDK 域名仍存在规则层硬拦截，可能抢先于脚本或 Map Local 执行。
- 部分接口返回空 JSON，兼容性不够稳。

## v4.0 - 2026-08-13

### 改动摘要

- 省电优化前的基准版本。
- 基于原 v3 模块中文化标题、简介、作者说明和版本注释。
- 保持 `[Rule]`、`[Script]` 等 Shadowrocket 固定语法不翻译。

## 后续小版本记录模板

复制下面内容追加到本文件顶部即可：

~~~md
## v4.x - YYYY-MM-DD

### 改动摘要

- 

### 涉及文件

- 

### 风险与兼容性

- 

### 校验结果

- 
~~~
