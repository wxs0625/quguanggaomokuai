# v4.52 精简范围自检

- 版本目录包含 1 个中文名 Shadowrocket 模块文件、1 个中文名京东启动脚本和 3 份中文 Markdown 记录。
- URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六段配置结构保持存在。
- 全局规则位于 Map Local 专用规则之后，只匹配明确的开屏和启动路径特征。
- 全局规则不包含 `DOMAIN-SUFFIX`、`DOMAIN-KEYWORD` 或整个软件域名拦截。
- 未加入 `hostname = *`，不会强制解密所有软件的 HTTPS 流量。
- 全局响应固定为 HTTP 200、JSON 空对象，避免普通连接拒绝触发应用内置广告回退。
- 京东本地启动脚本、京东业务直连例外和纵横小说专用规则保持存在。
- 未新增 v4.7 全量域名，未修改 v4.51 及之前版本目录。
- 已完成文本静态检查；软件冷启动、首页加载和网络功能仍需 iPhone/Shadowrocket 实机验证。
