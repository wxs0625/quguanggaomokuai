# v4.42 精简范围自检

- 来源范围：v4.41 精简范围恢复版。
- 未使用 v4.7 全量域名。
- 未新增 App 或恢复已删除模块。
- 保留 v4.41 六段配置结构。
- `frontier-aweme-hj-ipainner.amemv.com` 仅新增前置 `DIRECT`。
- 未宽泛放行全部 `frontier-aweme-*`、`amemv.com`、`snssdk.com` 或 `zijieapi.com`。
- 5 类 `api100-*.amemv.com` 规则均为 `DIRECT`，无对应 `REJECT`。
- 保留 `DOMAIN-KEYWORD,bds.snssdk.com,REJECT`。
- 保留 `DOMAIN-KEYWORD,p3.pstatp.com,REJECT`。
- 保留抖音 `/aweme/v*/ad/` 与 `/api/ad/` 广告接口拦截。
- v4.42 脚本标签使用 `v442_`，未残留 `v441_` 标签。
- v4.41、v4.40 及之前版本目录未修改。
