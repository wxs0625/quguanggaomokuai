# v4.42 精简范围自检

- 来源范围：v4.23 用户精简版。
- 未使用 v4.7 全量域名。
- 修正 `cupid.51job*.comhostname` 拼接错误。
- 保留 v4.36 六段配置结构。
- 保留米游社、虎牙直播、腾讯视频、优酷以及其它已选 App 规则。
- 未恢复未安装 App 模块。
- 已声明 YouTubeNoAd 的 5 个参数及默认值。
- YouTube 响应脚本仅保留 1 条，另保留 2 条请求脚本。
- `[MITM]` 保留 `*.googlevideo.com` 和 `youtubei.googleapis.com`。
- 网易云 `xeapi/ad/get` 合法空广告结构和 `eapi/link/position/show/resource` 空响应继续保留。
- 汽水硬拒绝规则不再覆盖 `api3-lq.qishui.com`，api3 广告接口改为响应净化。
- 汽水 `api3-lq.qishui.com` 继续位于 `[MITM]`，歌曲页和内容响应规则保留。
- 微博新增首页时间线、发现页、热搜、话题、评论和详情响应脚本。
- `[MITM]` 已移除微博排除项并加入 `*.weibo.cn`、`*.weibo.com` 和 `weibo.com`。
- v4.42 新增脚本标签使用 `v442_`，v4.41 的 `v441_` 脚本保持不变。
- 哔哩哔哩、抖音、闪动校园和其它应用规则未改动。
- 抖音核心接口放行规则位于共享广告拒绝规则之前。
