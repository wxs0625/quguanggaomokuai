# v4.44 精简范围自检

- 来源范围：v4.43 精简范围恢复版。
- 未使用 v4.7 全量域名。
- 未新增 App 或恢复已删除模块。
- 保留 v4.43 六段配置结构。
- `p3-developer.bytemaimg.com` 仅新增前置 `DIRECT`。
- 不存在 `DOMAIN,p3-developer.bytemaimg.com,REJECT` 残留。
- 未宽泛放行全部 `bytemaimg.com`、`amemv.com`、`snssdk.com` 或 `zijieapi.com`。
- 保留 `log.snssdk.com`、`dig.bdurl.net`、`zlink.ugsdk.cn`、`ma.zijieapi.com` 和 `mon.zijieapi.com` 的拒绝规则。
- 保留 v4.43 已确认的五个抖音必要主机直连规则。
- 保留抖音广告接口和广告素材拦截。
- v4.44 脚本标签使用 `v444_`，未残留 `v443_` 标签。
- v4.43、v4.42、v4.41 及 v4.40 之前版本目录未修改。
