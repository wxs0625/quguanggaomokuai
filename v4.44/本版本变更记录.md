# v4.44 本版本变更记录

生成时间：2026-08-19

版本定位：基于 v4.43，依据用户提供的 `proxy-2026-08-19-104837.db`，修复抖音暂停识图仍提示无网络的问题。

## 日志核对

- 数据库记录时间为 `2026-08-19 10:48:42` 至 `10:48:55`，共 122 条连接记录。
- 抖音搜索和识图链路中的 `search5-search-*.amemv.com`、`tsearch.amemv.com`、`search3-search-lq.toutiaoapi.com`、多个 `douyinpic.com` 图片签名主机均为 `DIRECT`。
- `frontier-aweme-lf-ipainner.amemv.com` 也已为 `DIRECT`，说明 v4.43 对上一份日志中识别出的主机修复已经生效。
- `p3-developer.bytemaimg.com:443` 在 `10:48:54` 连续 4 次命中 `DOMAIN,p3-developer.bytemaimg.com,REJECT`。
- 该拒绝发生在搜索和图片签名请求之后，且日志中没有该主机的成功连接记录，是本次日志中唯一紧邻识图功能后续阶段的功能资源主机。
- `log.snssdk.com`、`dig.bdurl.net`、`zlink.ugsdk.cn`、`ma.zijieapi.com`、`mon.zijieapi.com` 仍按日志分类保持拒绝；这些是日志、统计、广告或监测流量，不能据此放行。

## 本次处理

1. 在抖音放行区新增 `DOMAIN,p3-developer.bytemaimg.com,DIRECT`。
2. 删除后方同主机的 `REJECT`，避免模块内出现相反策略。
3. 保留 v4.43 的 `frontier-aweme-lf-ipainner`、`vcs`、TNC 和 `bytegecko` 精确直连。
4. 保留抖音广告域名、广告接口、广告素材及统计拦截，不放行全部 `bytemaimg.com` 或字节域名。
5. 其它 App 规则、精简范围和 MITM 配置继承 v4.43。
6. 同步更新模块名称、简介、注释和网易云脚本标签为 v4.44。

## 验证结论

- `p3-developer.bytemaimg.com` 只有 1 条前置 `DIRECT`，没有同主机 `REJECT` 残留。
- v4.43 已确认的抖音必要主机直连规则保持存在。
- `log.snssdk.com`、`dig.bdurl.net`、`zlink.ugsdk.cn`、`ma.zijieapi.com` 和 `mon.zijieapi.com` 仍保持拒绝。
- 模块保持 URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六个配置段。
- v4.43、v4.42、v4.41 及 v4.40 之前版本目录未修改。
- 已完成日志和规则静态核对；无法在当前环境替代 iPhone 运行测试。

