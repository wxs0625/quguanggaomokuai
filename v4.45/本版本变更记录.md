# v4.45 本版本变更记录

生成时间：2026-08-19

版本定位：基于 v4.44，依据用户提供的 `proxy-2026-08-19-113807.db`，修复抖音暂停识图仍提示无网络的问题。

## 日志核对

- 数据库记录时间为 `2026-08-19 11:38:14` 至 `11:38:23`，共 92 条连接记录。
- 抖音搜索和识图链路中的 `search5-search-*.amemv.com`、`tsearch.amemv.com`、`search3-search-lq.toutiaoapi.com`、多个 `douyinpic.com` 图片签名主机均为 `DIRECT`。
- `frontier-aweme-lf-ipainner.amemv.com` 也已为 `DIRECT`，说明 v4.43 对上一份日志中识别出的主机修复已经生效。
- `p3-ad-sign.byteimg.com:443` 和 `p9-ad-sign.byteimg.com:443` 在 `11:38:16` 各连续命中 2 次 `AND,((DOMAIN-KEYWORD,-ad-),(DOMAIN-SUFFIX,byteimg.com)),REJECT`。
- 两个主机发生在 `lf-webcast-gr-sourcecdn.bytegecko.com` 之后、`p3-c-sign.douyinpic.com` 等识图图片请求之前；本次日志没有它们的成功连接记录。
- v4.44 已放行的 `p3-developer.bytemaimg.com` 未再次出现，说明上一处阻断已不再命中。
- `log.snssdk.com`、`dig.bdurl.net`、`zlink.ugsdk.cn`、`ma.zijieapi.com`、`mon.zijieapi.com` 仍按日志分类保持拒绝；这些是日志、统计、广告或监测流量，不能据此放行。

## 本次处理

1. 在抖音放行区新增 `DOMAIN,p3-ad-sign.byteimg.com,DIRECT`。
2. 在抖音放行区新增 `DOMAIN,p9-ad-sign.byteimg.com,DIRECT`。
3. 将两条精确直连放在共享 `-ad- + byteimg.com` 拒绝规则之前，使其它 `byteimg.com` 广告主机继续拦截。
4. 保留 v4.44 的抖音核心接口、`p3-developer`、TNC 和图片资源直连。
5. 其它 App 规则、精简范围和 MITM 配置继承 v4.44。
6. 同步更新模块名称、简介、注释和网易云脚本标签为 v4.45。

## 验证结论

- `p3-ad-sign.byteimg.com`、`p9-ad-sign.byteimg.com` 各只有 1 条前置 `DIRECT`。
- 两个精确主机没有前置 `REJECT`；其它宽泛 `byteimg.com` 广告规则保持不变。
- v4.43 已确认的抖音必要主机直连规则保持存在。
- `log.snssdk.com`、`dig.bdurl.net`、`zlink.ugsdk.cn`、`ma.zijieapi.com` 和 `mon.zijieapi.com` 仍保持拒绝。
- 模块保持 URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六个配置段。
- v4.44、v4.43、v4.42、v4.41 及 v4.40 之前版本目录未修改。
- 已完成日志和规则静态核对；无法在当前环境替代 iPhone 运行测试。
