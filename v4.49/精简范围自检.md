# v4.49 精简范围自检

- 来源范围：v4.48 精简范围恢复版。
- 未使用 v4.7 全量域名，未新增 App 或恢复已删除模块。
- URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六段配置结构完整。
- 京东保留上游 `functionId=getWidgetV1052` 直播小窗规则。
- 京东不再存在额外 Map Local、Body Rewrite 或重复的 `JD_remove_ads.js` 脚本。
- 京东 Rule 保留上游基础规则，删除 `dsp-x`、`im-x`、`xlog`、`janapi`、`dgstatic`、`jdqd` 和诊断 URL 等额外拒绝项。
- 京东 MITM 中只保留 `api.m.jd.com`、`dns.jd.com`；未保留 `m.360buyimg.com`、`jdread-api.jd.com`、`router-app-api.jdcloud.com` 等额外主机。
- 抖音仍只有 `bds.snssdk.com`、`p3.pstatp.com` 两条开屏拦截规则。
- 非京东、非抖音的其它规则继承 v4.48。
- 已完成配置静态核对，京东小模块需在 iPhone 实机逐项验证。
- v4.48 及之前版本目录未修改。
