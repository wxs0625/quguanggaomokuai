# v4.49 精简保留说明

本版本继续使用 v4.40 的精简 App 范围，不恢复 v4.7 全量域名，也不新增未安装 App。

京东部分以 `fmz200/wool_scripts` 的独立京东模块为基线：保留基础广告域名拦截、直播小窗拦截、上游京东脚本，以及 `api.m.jd.com`、`dns.jd.com` 的 MITM。删除会返回空数据、改写基础配置或重复处理同一响应的整合扩展规则，优先恢复小模块的网络和数据加载。

京喜脚本仍保留，因为它匹配独立的京喜接口；其它 App 规则继承 v4.48。抖音仍只保留两条开屏广告拦截规则，不恢复抖音共享字节系去广告。
