# v4.41 精简范围自检

- 来源范围：v4.23 用户精简版。
- 未使用 v4.7 全量域名。
- 修正 `cupid.51job*.comhostname` 拼接错误。
- 保留 v4.36 六段配置结构。
- 保留米游社、虎牙直播、腾讯视频、优酷以及其它已选 App 规则。
- 未恢复未安装 App 模块。
- 已声明 YouTubeNoAd 的 5 个参数及默认值。
- YouTube 响应脚本仅保留 1 条，另保留 2 条请求脚本。
- `[MITM]` 保留 `*.googlevideo.com` 和 `youtubei.googleapis.com`。
- 网易云日志确认的 `xeapi/ad/*` 继续拦截，并新增 `xeapi/ad/get` 合法空广告结构。
- 网易云 `eapi/link/position/show/resource` 已加入空响应，覆盖本次启动阶段反复出现的接口。
- 汽水规则覆盖日志中的 `api3-lq.qishui.com`，并已加入 `[MITM]`。
- v4.41 脚本标签使用 `v441_`，未残留 `v440_` 汽水或网易云脚本标签。
- 哔哩哔哩、抖音、闪动校园和其它应用规则未改动。
- 抖音核心接口放行规则位于共享广告拒绝规则之前。
