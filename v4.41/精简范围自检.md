# v4.41 精简范围自检

- 来源范围：v4.40 精简范围恢复版。
- 未使用 v4.7 全量域名。
- 未新增 App 或恢复已删除模块。
- 保留 v4.40 六段配置结构。
- 5 类 `api100-*.amemv.com` 规则均为前置 `DIRECT`。
- 不存在上述 5 类主机的 `REJECT` 规则。
- 保留 `DOMAIN-KEYWORD,bds.snssdk.com,REJECT`。
- 保留 `DOMAIN-KEYWORD,p3.pstatp.com,REJECT`。
- 保留抖音 `/aweme/v*/ad/` 与 `/api/ad/` 广告接口拦截。
- 未宽泛放行全部 `amemv.com`、`snssdk.com` 或 `zijieapi.com`。
- v4.41 脚本标签使用 `v441_`，未残留 `v440_` 标签。
- v4.40 及之前版本目录未修改。
