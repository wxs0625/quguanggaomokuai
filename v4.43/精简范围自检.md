# v4.43 精简范围自检

- 来源范围：v4.42 精简范围恢复版。
- 使用用户提供的 `proxy-2026-08-19-102611.db` 进行规则定位。
- 未使用 v4.7 全量域名。
- 未新增 App 或恢复已删除模块。
- 保留 v4.42 六段配置结构。
- `frontier-aweme-lf-ipainner.amemv.com` 仅新增前置 `DIRECT`。
- `vcs.zijieapi.com` 仅新增前置 `DIRECT`，旧 `REJECT` 位于其后。
- `tnc0-alisc1.zijieapi.com` 仅新增前置 `DIRECT`。
- `lf-normal-gr-sourcecdn.bytegecko.com` 仅新增前置 `DIRECT`。
- `lf-webcast-gr-sourcecdn.bytegecko.com` 仅新增前置 `DIRECT`。
- 未宽泛放行全部 `frontier-aweme-*`、`zijieapi.com`、`tnc`、`amemv.com` 或 `snssdk.com`。
- 保留 `DOMAIN,log.snssdk.com,REJECT`、`DOMAIN,zlink.ugsdk.cn,REJECT` 和 `DOMAIN,dig.bdurl.net,REJECT`。
- 保留抖音 `/aweme/v*/ad/` 与 `/api/ad/` 广告接口拦截。
- v4.43 脚本标签使用 `v443_`，未残留 `v442_` 标签。
- v4.42、v4.41、v4.40 及之前版本目录未修改。
