# v4.43 本版本变更记录

生成时间：2026-08-19

版本定位：基于 v4.42，依据用户提供的 `proxy-2026-08-19-102611.db` 修复抖音暂停识图仍提示网络错误。

## 日志核对

- 日志文件大小 `77824` 字节，记录时间为 `2026-08-19 10:26:16` 至 `10:26:27`。
- `frontier-aweme-lf-ipainner.amemv.com:443` 被共享 `frontier-aweme-*` 规则拒绝 7 次。
- `vcs.zijieapi.com:443` 被 `DOMAIN,vcs.zijieapi.com,REJECT` 拒绝 6 次。
- 抖音 `app_name=aweme` 的 `https://tnc0-alisc1.zijieapi.com/get_domains/v5/` 被共享 TNC 规则拒绝 1 次。
- `lf-normal-gr-sourcecdn.bytegecko.com:443` 被拒绝 4 次，`lf-webcast-gr-sourcecdn.bytegecko.com:443` 被拒绝 1 次；两者是抖音正常资源 CDN 请求。
- 三类请求均使用 iPhone Cronet User-Agent，时间集中在用户复现网络错误期间。
- `log.snssdk.com`、`zlink.ugsdk.cn`、`dig.bdurl.net` 和日志中其它统计/广告域名仍保持拒绝，没有纳入放行。

## 本次处理

1. 在共享拒绝规则之前新增 `DOMAIN,frontier-aweme-lf-ipainner.amemv.com,DIRECT`。
2. 在共享拒绝规则之前新增 `DOMAIN,vcs.zijieapi.com,DIRECT`。
3. 在共享 TNC 拒绝规则之前新增 `DOMAIN,tnc0-alisc1.zijieapi.com,DIRECT`。
4. 在对应资源拒绝规则之前新增两个 `bytegecko.com` 正常资源 CDN 精确直连。
5. 保留 v4.42 已确认的 `frontier-aweme-hj-ipainner.amemv.com` 和 5 类 `api100` 精确放行。
6. 不放行全部 `frontier-aweme-*`、`zijieapi.com`、`tnc` 或字节域名。
7. 保留抖音广告域名、广告接口、广告素材、日志和统计拦截。
8. 其它 App 规则、精简范围和 MITM 配置继承 v4.42。
9. 同步更新模块名称、简介、注释和网易云脚本标签为 v4.43。

## 验证结论

- 五个新增主机各只有 1 条前置 `DIRECT`；`vcs.zijieapi.com` 的旧拒绝行位于其后，不会先于直连命中。
- 五个新增直连规则均位于相关共享拒绝规则之前。
- `log.snssdk.com`、`zlink.ugsdk.cn` 和 `dig.bdurl.net` 仍为拒绝。
- 模块保持 URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六个配置段。
- v4.42 文件夹未修改，v4.40 及之前版本未修改。
- 规则和日志静态核对已完成；尚未取得安装 v4.43 后的新设备日志，不能声称已在 iPhone 上实测通过。
