# v4.47 精简保留说明

本版本继续使用 v4.40 的精简 App 范围，不恢复 v4.7 全量模块，也不新增未安装 App。

本次依据最新日志和用户确认，新增两个识物流程精确直连主机：`log.snssdk.com` 与 `dig.bdurl.net`。

抖音搜索主机、识图服务、图片签名主机、`p3-developer.bytemaimg.com`、`p3-ad-sign.byteimg.com`、`p9-ad-sign.byteimg.com`、`frontier-aweme-lf-ipainner`、TNC 和 bytegecko 直连规则继承 v4.46。其它统计域名和共享广告拒绝规则仍保留，只有本次日志确认的两个精确主机直连。外部独立模块 `128d699456479d6ea0f537a0e3e4eb97.sgmodule` 的 `dig.bdurl.net,REJECT` 必须关闭，否则会抵消本版本修复。

哔哩哔哩、网易云音乐、闪动校园及其它已保留 App 的处理规则均继承 v4.45。
