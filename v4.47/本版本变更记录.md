# v4.47 本版本变更记录

生成时间：2026-08-19

版本定位：基于 v4.46，依据用户提供的 `proxy-2026-08-19-130823.db` 及用户确认，修复抖音暂停后使用“识物”时因末端请求被拦截而提示无网络的问题。

## 日志核对

- 数据库记录时间为 `2026-08-19 13:08:27` 至 `13:08:37`，共 64 条连接记录。
- 抖音核心接口、`frontier-aweme-lf-ipainner.amemv.com`、`vcs.zijieapi.com`、`lf-normal-gr-sourcecdn.bytegecko.com`、`lf-webcast-gr-sourcecdn.bytegecko.com` 和 `douyinpic.com` 资源请求均为 `DIRECT`。
- `dig.bdurl.net` 在流程开始时被 `DOMAIN,dig.bdurl.net,REJECT` 拒绝 1 次。
- `log.snssdk.com` 在流程中被 `DOMAIN,log.snssdk.com,REJECT` 拒绝 11 次，其中最后两次发生在 `13:08:37`；用户确认识物随后提示无网络。
- 除上述两个精确主机外，本份日志没有其它 `REJECT` 主机，因此本版本只处理这两个可定位的拒绝点。

## 本次处理

1. 新增 `DOMAIN,log.snssdk.com,DIRECT`，让识物末端请求走直连。
2. 新增 `DOMAIN,dig.bdurl.net,DIRECT`，让同一识物流程的前置请求走直连。
3. 删除 v4.47 内部对这两个主机的后置 `REJECT`，避免模块内部规则重复冲突。
4. 保留 `extlog.snssdk.com`、`xlog.snssdk.com` 及其它未被本次日志确认的统计、广告域名拒绝。
5. 核对发现外部独立模块 `128d699456479d6ea0f537a0e3e4eb97.sgmodule` 仍拒绝 `dig.bdurl.net`；使用 v4.47 时必须关闭该旧独立模块，否则它仍可能在全局规则中拦截。
6. 其它 App 规则、精简范围和 MITM 配置继承 v4.46。

## 验证结论

- `log.snssdk.com`、`dig.bdurl.net` 各仅保留 1 条 `DIRECT`，v4.47 内部无同名 `REJECT`。
- `extlog.snssdk.com`、`xlog.snssdk.com`、`zlink.ugsdk.cn`、`ma.zijieapi.com` 和 `mon.zijieapi.com` 仍保持拒绝。
- v4.46 已确认的抖音识图必要主机直连规则保持存在。
- 已确认外部 `128d699456479d6ea0f537a0e3e4eb97.sgmodule` 存在 `dig.bdurl.net,REJECT` 冲突。
- 模块保持 URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六个配置段。
- v4.46 及之前版本目录未修改。
- 已完成日志和规则静态核对；无法在当前环境替代 iPhone 运行测试。
