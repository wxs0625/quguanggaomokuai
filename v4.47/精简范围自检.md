# v4.47 精简范围自检

- 来源范围：v4.46 精简范围恢复版。
- 未使用 v4.7 全量域名。
- 未新增 App 或恢复已删除模块。
- 保留 v4.45 六段配置结构。
- `log.snssdk.com` 和 `dig.bdurl.net` 各仅保留精确 `DIRECT`，模块内部无同名 `REJECT`。
- `vc-brain.ndcpp.com`、`p3-developer-sign.bytemaimg.com`、`p3-ad-sign.byteimg.com` 和 `p9-ad-sign.byteimg.com` 的精确前置 `DIRECT` 保持存在。
- 未宽泛放行全部 `bytemaimg.com`、`amemv.com`、`snssdk.com` 或 `zijieapi.com`。
- 保留 `extlog.snssdk.com`、`xlog.snssdk.com`、`zlink.ugsdk.cn`、`ma.zijieapi.com` 和 `mon.zijieapi.com` 的拒绝规则。
- 外部独立模块 `128d699456479d6ea0f537a0e3e4eb97.sgmodule` 的 `dig.bdurl.net,REJECT` 已确认会与本版本冲突，使用前需关闭。
- 保留 v4.45 已确认的抖音必要主机直连规则。
- 保留抖音广告接口和广告素材拦截。
- v4.47 脚本标签使用 `v447_`，未残留 `v446_` 标签。
- v4.46 及之前版本目录未修改。
