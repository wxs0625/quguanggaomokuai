# v4.54 精简范围自检

## 文件检查

- [x] 版本目录为 v4.54。
- [x] 模块文件使用中文名称并包含 v4.54。
- [x] 京东启动脚本存在。
- [x] 三份中文 Markdown 记录存在。
- [x] 旧版本目录未删除。

## 配置段检查

- [x] URL Rewrite 存在。
- [x] Map Local 存在。
- [x] Body Rewrite 存在。
- [x] Script 存在。
- [x] Rule 存在。
- [x] MITM 存在。

## 全局规则正例

- [x] 明确广告主机，例如 ad.example.com。
- [x] 明确开屏路径，例如 /splash、/startupAd、/openScreenAd。
- [x] 明确广告查询参数，例如 ?adspace=launch。
- [x] 多参数广告位，例如 ?foo=1&launchAd=true。

## 全局规则反例

- [x] startupConfig 不应仅因包含 startup 被拦截。
- [x] address 不应因包含 ad 被拦截。
- [x] /home/promotion 不应因 promotion 被拦截。
- [x] ?q=advertisement 不应因普通搜索词被拦截。
- [x] ?mode=ad 不应因普通参数值为 ad 被拦截。
- [x] 普通首页、搜索和播放 URL 不应被全局规则拦截。

## 说明

静态检查不能证明每个软件的冷启动广告都已消失。首次启动广告可能使用动态主机、加密参数、预加载缓存或应用内本地资源；如果 v4.54 仍有特定软件广告，应记录软件名称、版本和发生时间，再增加该软件的专用规则，避免继续扩大全局拦截范围。
