# v4.4 本版本变更记录

## 版本定位

v4.4 是版本归档发布版，开始执行“每个小版本一个根目录文件夹”的管理规则。

## 本版本文件

- `全能去广告修复版-Shadowrocket-v4.4.sgmodule`

## 改动内容

- 在根目录创建 `v4.0`、`v4.1`、`v4.2`、`v4.3`、`v4.4` 版本文件夹。
- 每个版本文件夹内保留该版本对应模块文件。
- 每个版本文件夹内新增 `本版本变更记录.md`，记录该版本用途、改动和检查重点。
- 恢复 v4.2 中曾经存在的闪动校园单独测试模块，仅作为历史归档保留。
- 当前推荐版本升级为 v4.4，继续保持单主模块发布。

## GitHub 上传策略

- 本次只准备本地版本归档和上传说明。
- 未经明确授权，不执行 `git push`。
- 授权上传前需要确认 GitHub 仓库地址。

## 校验结果

- 历史版本不再删除，而是保留在对应版本目录中。
- 当前推荐模块为 `v4.4/全能去广告修复版-Shadowrocket-v4.4.sgmodule`。
- 主模块结构段落完整。
