# v4.46 本版本变更记录

生成时间：2026-08-19

版本定位：基于 v4.45，依据用户提供的 `proxy-2026-08-19-123528.db`，继续修复抖音暂停识图仍提示无网络的问题。

## 日志核对

- 数据库记录时间为 `2026-08-19 12:35:28` 至 `12:35:50`，共 197 条连接记录。
- 抖音搜索和识图链路中的 `search5-search-*.amemv.com`、`tsearch.amemv.com`、`search3-search-lq.toutiaoapi.com`、多个 `douyinpic.com` 图片签名主机均为 `DIRECT`。
- `frontier-aweme-lf-ipainner.amemv.com` 也已为 `DIRECT`，说明 v4.43 对上一份日志中识别出的主机修复已经生效。
- v4.45 已修复的 `p3-ad-sign.byteimg.com`、`p9-ad-sign.byteimg.com` 本次没有再次被拒绝，说明上一处规则冲突已解除。
- `vc-brain.ndcpp.com:9681` 在 `12:35:31` 出现 2 次，类型为 `UDP Relay`，但仅命中 `GEOIP,CN,DIRECT`；这是本次日志中明确的识图服务主机。
- `p3-developer-sign.bytemaimg.com:443` 在 `12:35:38` 出现，类型为 HTTPS，但仅命中 `GEOIP,CN,DIRECT`；它紧接着识图图片请求出现。
- 后续 `p3-c-sign.douyinpic.com`、`p95-zjwztc-sign.douyinpic.com`、`p95-bj-sign.douyinpic.com` 等图片主机均为直连，说明本次重点是前两台服务依赖的精确路由。
- `log.snssdk.com`、`dig.bdurl.net`、`zlink.ugsdk.cn`、`ma.zijieapi.com`、`mon.zijieapi.com` 仍按日志分类保持拒绝；这些是日志、统计、广告或监测流量，不能据此放行。

## 本次处理

1. 在抖音放行区新增 `DOMAIN,vc-brain.ndcpp.com,DIRECT`，固定识图 UDP 服务走直连。
2. 在抖音放行区新增 `DOMAIN,p3-developer-sign.bytemaimg.com,DIRECT`，固定识图资源签名服务走直连。
3. 保留 v4.45 对 `p3-ad-sign.byteimg.com`、`p9-ad-sign.byteimg.com` 的精确直连。
4. 保留其它 `byteimg.com` 广告拦截和统计、监测域名拒绝规则。
5. 其它 App 规则、精简范围和 MITM 配置继承 v4.45。
6. 同步更新模块名称、简介、注释和网易云脚本标签为 v4.46。

## 验证结论

- `vc-brain.ndcpp.com`、`p3-developer-sign.bytemaimg.com` 各只有 1 条前置 `DIRECT`。
- `p3-ad-sign.byteimg.com`、`p9-ad-sign.byteimg.com` 各只有 1 条前置 `DIRECT`。
- 以上四个精确主机没有前置 `REJECT`；其它宽泛广告规则保持不变。
- v4.43 已确认的抖音必要主机直连规则保持存在。
- `log.snssdk.com`、`dig.bdurl.net`、`zlink.ugsdk.cn`、`ma.zijieapi.com` 和 `mon.zijieapi.com` 仍保持拒绝。
- 模块保持 URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六个配置段。
- v4.45、v4.44、v4.43、v4.42、v4.41 及 v4.40 之前版本目录未修改。
- 已完成日志和规则静态核对；无法在当前环境替代 iPhone 运行测试。
