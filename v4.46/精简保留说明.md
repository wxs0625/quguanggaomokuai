# v4.46 精简保留说明

本版本继续使用 v4.40 的精简 App 范围，不恢复 v4.7 全量模块，也不新增未安装 App。

本次新增日志确认的两个精确识图服务主机：`vc-brain.ndcpp.com` 与 `p3-developer-sign.bytemaimg.com`。

抖音搜索主机、图片签名主机、`p3-developer.bytemaimg.com`、`p3-ad-sign.byteimg.com`、`p9-ad-sign.byteimg.com`、`frontier-aweme-lf-ipainner`、TNC 和 bytegecko 直连规则继承 v4.45。共享 `-ad- + byteimg.com` 拒绝规则仍保留，只有日志确认的精确主机优先直连。

哔哩哔哩、网易云音乐、闪动校园及其它已保留 App 的处理规则均继承 v4.45。
