# v4.50 精简范围自检

- 来源范围：v4.49 精简范围恢复版。
- 未使用 v4.7 全量域名，未新增 App 或恢复已删除模块。
- URL Rewrite、Map Local、Body Rewrite、Script、Rule、MITM 六段配置结构完整。
- 京东无 `dns.jd.com`、`101.124.19.122/32`、`2402:db40:5100:1011::5/128` 的拒绝规则，MITM 中也无 `dns.jd.com`。
- 京东 v4.49 已确认的 13 条基础域名规则、直播小窗规则和 `jingdong.js` 脚本保持存在。
- 汽水音乐 URL Rewrite 只剩 `api.qishui.com`、`api5-lq.qishui.com` 的 `/luna/ads/` 一条精确广告接口规则。
- 无 `beta-luna.douyin.com` 的 URL Rewrite、Map Local、Body Rewrite、Script、Rule 或 MITM 项目。
- 无汽水音乐 `SodaMusic_remove_ads.js` 脚本与模式、播放器、会员或活动接口改写。
- 抖音仍只有 `bds.snssdk.com`、`p3.pstatp.com` 两条开屏拦截规则。
- 已完成配置静态核对，京东小组件与汽水音乐模式切换需在 iPhone 实机验证。
- v4.49 及之前版本目录未修改。
