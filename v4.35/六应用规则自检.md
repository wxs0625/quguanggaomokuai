# v4.35 六应用规则自检

## 自检目标

- 六个 App 的单独模块有效规则都位于对应段落前部。
- 腾讯视频和汽水音乐脚本名改为全局唯一名称。
- 优酷 `push.m.youku.com`、`un-acs.youku.com` 的 DIRECT 规则位于优酷广告域名 REJECT 之前。
- 删除 v4.34 对优酷开屏接口的整段空响应和旧 `cnftp.js` 开屏处理。
- 六个 App 仅加入实际使用的域名，不恢复其它未安装 App 的域名。
- 全模块仍保持一个文件、每种配置段只出现一次。
